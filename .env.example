# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# Application Settings
DATABASE_PATH=./data/analysis.db
LOG_LEVEL=INFO
MAX_PARALLEL_TASKS=5

# MCP Settings (Yahoo Finance - no key needed)
# The system tries 5 free MCP servers in priority order:
# 1. @modelcontextprotocol/server-yahoo-finance (primary)
# 2. @agentx-ai/mcp-server-yahoo-finance
# 3. @alex2yang97/mcp-yahoo-finance
# 4. @leoncuhk/mcp-yahoo-finance
# 5. @zentickr/yahoo-query-mcp
# If all MCP servers fail, falls back to direct yfinance library
MCP_YFINANCE_ENABLED=true
